// import connect from "../db.js";
// import express from "express";
// import mongoose from "mongoose";
// import { Kitten } from "../models/kitten.js";
// import User from "../models/user.js";

// mockDB?

describe("database", () => {
  it.todo("runs the connect function");
  it.todo("connects successfully");
  // expect.assertions(1);
  // const mockConnect = jest.fn(connectDB);
  // const mockConnect = connectDB;
  // const response = "Connected to the database!";
  // try {
  //   return expect(response).toBe(response);
  // const res = await mockConnect.mockResolvedValue(response);
  // return expect(connect()).resolves.toStrictEqual(response);
  // } catch (error) {
  //   console.error(error);
  //   return error;
  // }
  // await mockConnect().then((res) => {
  //   console.log(res);
  //   const { connection } = res;
  //   // eslint-disable-next-line jest/no-conditional-expect
  // }).catch((err) => console.error(err));
  // });
  // expect(connection).toBe("Connected to the database!");
  it.todo("disconnects successfully");
});
